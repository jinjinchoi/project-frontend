export interface IExistCookie {
    isLogin : boolean;
}

export interface ICookieUserInfo {
    id : number;
    uid : string;
    unickname : string;
    uemail : string;
    uphoen : number;
    isAdmin : boolean;
    uprofile : string;
}